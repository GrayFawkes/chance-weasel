var uploadFile = function () {
			// debugger;
			// var file = event.target.files[0];
			// var reader = new FileReader();
			// $scope[id] = {};
			// $scope[id].name = file.name;
			// $scope[id].id = id;
			// $scope.safeDigest();
	
			// // Set up a callback to be trigerred when upload is complete
			// reader.onloadstart = (function (l) {
			// 	return function (e) {
			// 		$scope[id].onload = true;
					
			// 		$scope.$broadcast('uploadPending', $scope[id]); // Tell everyone that there is a pending upload
			// 		$scope.safeDigest();
			// 	}
			// })($scope[id]);
			// reader.onload = (function (l) {
			// 	return function (e) { l.CONTENT = e.target.result;}
			// })($scope[id]);
			// reader.onloadend = (function (l) {
			// 	return function (e) {
			// 		$scope[id].onload = false;
			// 		$scope.$broadcast('uploadCompleted', l); // Tell everyone that the upload is complete
			// 		$scope.safeDigest();
			// 	}
			// })($scope[id]);
			
			// if ( id === 'excel' ) reader.readAsDataURL(file); // Start the upload
			// if ( id.substring(0,4) === 'mail' ) reader.readAsText(file); // Start the upload
}